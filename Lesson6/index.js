// index.js

const movieName = require('movie-name');
const fruitName = require('random-fruits-name');

const movie = movieName();
const fruit = fruitName(); 

console.log(movie);
console.log(fruit);
